import { createStore } from 'vuex';
import axios from 'axios';

export default createStore({
  state: {
    user: {
      isAuthenticated: false,
      token: '',
    },
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  },
});
